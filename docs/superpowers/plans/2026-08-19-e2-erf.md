# E2 ERF 정량 측정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 논문 3.1절의 "CMT는 등방, Vim은 scan 방향 이방" 주장을 세 개의 숫자로
재서 `results/e2/`에 남긴다.

**Architecture:** 사전학습 가중치를 받아 세 모델의 최종 특징맵 중심 토큰에 대한
입력 gradient를 VOC 이미지 위에서 평균해 224×224 ERF map을 만들고, 그 맵에서
비등방 지수·주축 각도·수직/수평 감쇠비를 계산한다. 판단이 들어가는 계산은 전부
순수 함수로 분리해 matplotlib과 GPU 없이 검증한다.

**Tech Stack:** torch 2.1.1+cu118, torchvision 0.16.1, timm 0.9.12, numpy, pandas,
matplotlib, pytest

설계 문서: `docs/superpowers/specs/2026-08-19-e2-erf-design.md`

## Global Constraints

- **측정값은 고정 환경에서만 나온다.** WSL2 conda 환경 `/opt/conda/envs/e1`.
  저장소 명령은 스크래치패드의 `wsl/run.sh` 래퍼로 실행한다. Windows에서 잰 수치를
  `results/`에 커밋하지 않는다.
- **테스트는 skip하지 않는다.** 환경이 없으면 하드 실패한다. 조용히 건너뛴 테스트는
  통과한 테스트처럼 보인다.
- **테스트가 자기가 막는다고 주장하는 회귀를 실제로 잡는지 확인한다.** 각 태스크
  마지막에 회귀를 주입해 해당 테스트가 실패하는 것을 보고, 되돌린다.
- **코드 주석과 docstring은 한글, 그림에 그려지는 문자열은 영어.** matplotlib 기본
  폰트에 한글 글리프가 없어 PNG에 네모 상자로 찍힌다.
- **커밋에 Claude 공동 저자 트레일러를 넣지 않는다.**
- 해상도는 224²만 쓴다.
- `results/`의 그림은 `results/`의 데이터만 읽는다. 손으로 옮겨 적은 숫자를 넣지
  않는다.

---

## File Structure

| 파일 | 책임 |
|------|------|
| `data/voc.py` | VOC 2012 다운로드, 고정 seed 샘플, 224² 텐서 로딩 |
| `models/checkpoints.py` | 체크포인트 URL 표, 캐시 다운로드, SHA256 |
| `models/cmt.py` (수정) | CMT-S 가중치 로딩 |
| `models/vim.py` (수정) | Vim-S 가중치 로딩 |
| `models/probes.py` | 모델별 "최종 특징맵 중심 토큰" 선택. **cls 토큰 배제** |
| `bench/erf.py` | ERF map 누적, 세 지표, 수렴 판정 (순수 함수 분리) |
| `experiments/e2_erf.py` | 3모델 × 3조건 실행, CSV·npz·env.json 기록 |
| `figures/e2_plot.py` | heatmap + 지표 + 수렴 곡선 |

---

### Task 1: VOC 2012 데이터 준비

**Files:**
- Create: `data/__init__.py`, `data/voc.py`
- Test: `tests/test_voc.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `ensure_voc(root: str | Path = "data") -> Path` — VOC2012 이미지 디렉터리 경로
  - `sample_image_paths(paths: list[Path], n: int, seed: int = 0) -> list[Path]`
  - `load_images(paths: list[Path], size: int = 224) -> torch.Tensor` — `(N, 3, size, size)`, ImageNet 정규화

`data/`는 `.gitignore`에 있지만 그건 내려받은 데이터셋을 가리킨다. `data/voc.py`는
코드이므로 커밋한다. `.gitignore`의 `data/` 규칙이 이 파일까지 무시하므로 Step 5에서
예외를 추가한다.

- [ ] **Step 1: 샘플링 테스트를 먼저 쓴다**

`tests/test_voc.py`:

```python
from pathlib import Path

import pytest
import torch

from data.voc import load_images, sample_image_paths


def _paths(n: int) -> list[Path]:
    return [Path(f"img_{i:04d}.jpg") for i in range(n)]


def test_same_seed_gives_the_same_sample():
    """이미지가 달라지면 ERF도 달라진다. 재현되지 않는 샘플은 측정을 무효로 만든다."""
    first = sample_image_paths(_paths(100), n=10, seed=0)
    second = sample_image_paths(_paths(100), n=10, seed=0)
    assert first == second


def test_different_seeds_give_different_samples():
    assert sample_image_paths(_paths(100), 10, seed=0) != sample_image_paths(
        _paths(100), 10, seed=1
    )


def test_sample_order_does_not_depend_on_input_order():
    """파일시스템 순회 순서는 OS마다 다르다. 정렬하지 않으면 같은 seed로도
    다른 이미지가 뽑힌다."""
    forward = sample_image_paths(_paths(100), 10, seed=0)
    backward = sample_image_paths(list(reversed(_paths(100))), 10, seed=0)
    assert forward == backward


def test_asking_for_more_than_exists_fails_loudly():
    """조용히 적게 반환하면 N=256으로 잰 줄 알았던 값이 실은 N=40이 된다."""
    with pytest.raises(ValueError, match="256"):
        sample_image_paths(_paths(40), n=256, seed=0)
```

- [ ] **Step 2: 실패를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_voc.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'data.voc'`

- [ ] **Step 3: 샘플링을 구현한다**

`data/__init__.py`는 빈 파일. `data/voc.py`:

```python
"""VOC 2012 이미지 준비. E2와 E3가 같은 이미지를 쓴다."""
import random
from pathlib import Path

import torch
from PIL import Image
from torchvision import transforms

IMAGENET_MEAN = (0.485, 0.456, 0.406)
IMAGENET_STD = (0.229, 0.224, 0.225)


def sample_image_paths(paths: list[Path], n: int, seed: int = 0) -> list[Path]:
    """고정 seed로 n장을 뽑는다.

    정렬을 먼저 하는 이유는 파일시스템 순회 순서가 OS·파일시스템마다 다르기
    때문이다. 정렬하지 않으면 같은 seed로도 다른 이미지가 뽑혀 재현이 깨진다.
    """
    if n > len(paths):
        raise ValueError(f"{n}장을 요청했는데 {len(paths)}장뿐이다")
    ordered = sorted(paths)
    return sorted(random.Random(seed).sample(ordered, n))


def load_images(paths: list[Path], size: int = 224) -> torch.Tensor:
    transform = transforms.Compose([
        transforms.Resize(size),
        transforms.CenterCrop(size),
        transforms.ToTensor(),
        transforms.Normalize(IMAGENET_MEAN, IMAGENET_STD),
    ])
    return torch.stack([
        transform(Image.open(path).convert("RGB")) for path in paths
    ])
```

- [ ] **Step 4: 통과를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_voc.py -q`
Expected: PASS (4건)

- [ ] **Step 5: 다운로드를 붙이고 `.gitignore` 예외를 넣는다**

`data/voc.py`에 추가:

```python
def ensure_voc(root: str | Path = "data") -> Path:
    """VOC2012 trainval(약 1.9GB)을 받고 JPEG 디렉터리를 돌려준다.

    torchvision이 체크섬까지 확인한다. 이미 받았으면 다시 받지 않는다.
    """
    from torchvision.datasets import VOCSegmentation

    root = Path(root)
    images = root / "VOCdevkit" / "VOC2012" / "JPEGImages"
    if not images.is_dir():
        VOCSegmentation(root=str(root), year="2012", image_set="val", download=True)
    if not images.is_dir():
        raise RuntimeError(f"VOC 다운로드 후에도 {images}가 없다")
    return images
```

`.gitignore`의 `data/` 줄을 아래로 바꾼다 — 데이터는 무시하되 코드는 추적한다:

```
data/*
!data/__init__.py
!data/voc.py
```

- [ ] **Step 6: 실제 다운로드를 한 번 돌려 확인한다**

Run:
```
wsl bash <래퍼> python -c "
from data.voc import ensure_voc, sample_image_paths, load_images
d = ensure_voc()
paths = sample_image_paths(list(d.glob('*.jpg')), 8, seed=0)
x = load_images(paths)
print(d, len(list(d.glob('*.jpg'))), x.shape, float(x.mean()), float(x.std()))
"
```
Expected: JPEG 17125장, `torch.Size([8, 3, 224, 224])`. 정규화 후 평균이 대략
−0.5 ~ 0.5, 표준편차가 대략 0.8 ~ 1.3이면 정상이다. 평균이 0.4~0.5 근처이고
표준편차가 0.2~0.3이면 정규화가 빠진 것이다.

- [ ] **Step 7: 커밋**

```bash
git add data/ tests/test_voc.py .gitignore
git commit -m "feat: draw a reproducible VOC sample E2 and E3 can share"
```

---

### Task 2: 체크포인트 다운로드와 무결성 기록

**Files:**
- Create: `models/checkpoints.py`
- Test: `tests/test_checkpoints.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `CHECKPOINTS: dict[str, Checkpoint]` — 키는 `"cmt_s"`, `"vim_s"`
  - `fetch(name: str, root: str | Path = "checkpoints") -> Path`
  - `sha256_of(path: Path) -> str`
  - `unwrap_state_dict(obj: dict) -> dict[str, torch.Tensor]`

**URL은 확인된 값이다** (2026-08-19 기준 HTTP 200):

| 모델 | URL | 크기 |
|------|-----|------|
| CMT-S | `https://github.com/ggjy/CMT.pytorch/releases/download/release-v1/cmt_small.pth` | 105,906,946 B |
| Vim-S | `https://huggingface.co/hustvl/Vim-small-midclstok/resolve/main/vim_s_midclstok_80p5acc.pth` | 413,390,910 B |

**설계 문서의 CMT 출처는 틀렸다.** spec과 README·HANDOFF는 huawei-noah/
Efficient-AI-Backbones Releases라고 적었지만 그 저장소의 릴리스 자산에 CMT가 없다.
실제 배포처는 원저자 개인 저장소 `ggjy/CMT.pytorch`이고, `cmt_pytorch/README.md`가
그리로 링크한다. **Task 2 마지막에 spec·README·HANDOFF의 이 문장을 함께 고친다.**

SHA256은 코드에 박지 않는다. 아직 받아 보지 않은 값을 게이트로 걸면 첫 실행에서
반드시 실패하고, 그때 하는 일은 실패한 값을 그대로 복사해 넣는 것뿐이라 검증이
되지 않는다. 대신 실행 시 계산해 `results/e2/env.json`에 **기록**한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_checkpoints.py`:

```python
import hashlib

import pytest
import torch

from models.checkpoints import CHECKPOINTS, fetch, sha256_of, unwrap_state_dict


def test_sha256_matches_hashlib(tmp_path):
    path = tmp_path / "blob.bin"
    path.write_bytes(b"cmt vs vim")
    assert sha256_of(path) == hashlib.sha256(b"cmt vs vim").hexdigest()


def test_an_existing_file_is_not_downloaded_again(tmp_path, monkeypatch):
    """394MB를 매 실행 다시 받으면 아무도 이 실험을 돌리지 않는다."""
    (tmp_path / "cmt_small.pth").write_bytes(b"already here")
    calls = []
    monkeypatch.setattr(
        "models.checkpoints._download", lambda url, dest: calls.append(url)
    )

    path = fetch("cmt_s", root=tmp_path)

    assert path.read_bytes() == b"already here"
    assert calls == []


def test_unwrap_finds_the_weights_under_either_wrapper():
    """체크포인트마다 감싸는 키가 다르다. 감싼 채로 load_state_dict에 넘기면
    '키가 하나도 안 맞는다'는 엉뚱한 오류가 난다."""
    weights = {"head.weight": torch.zeros(2, 2)}
    assert unwrap_state_dict({"model": weights}) is weights
    assert unwrap_state_dict({"state_dict": weights}) is weights
    assert unwrap_state_dict(weights) is weights


def test_unwrap_refuses_something_that_is_not_a_state_dict():
    """조용히 빈 dict를 돌려주면 strict=False였을 때 랜덤 가중치로 측정하게 된다."""
    with pytest.raises(ValueError, match="state_dict"):
        unwrap_state_dict({"epoch": 300, "args": {}})


def test_every_checkpoint_entry_has_a_url_and_a_filename():
    for name, entry in CHECKPOINTS.items():
        assert entry.url.startswith("https://"), name
        assert entry.filename.endswith(".pth"), name
```

- [ ] **Step 2: 실패를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_checkpoints.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'models.checkpoints'`

- [ ] **Step 3: 구현한다**

`models/checkpoints.py`:

```python
"""사전학습 체크포인트 확보와 무결성 기록.

SHA256을 코드에 박지 않는 이유: 아직 받아 보지 않은 값을 게이트로 걸면 첫 실행이
반드시 실패하고, 그때 하는 일은 실패한 값을 복사해 넣는 것뿐이라 아무것도 검증되지
않는다. 대신 실행마다 계산해 results/e2/env.json에 기록한다 — 나중에 다른 가중치로
잰 결과와 섞이는 것을 그 기록이 막는다.
"""
import hashlib
from dataclasses import dataclass
from pathlib import Path

import torch


@dataclass(frozen=True)
class Checkpoint:
    url: str
    filename: str
    reported_top1: float


# 2026-08-19 확인. CMT는 huawei-noah/Efficient-AI-Backbones 릴리스에 없다 —
# 그 저장소의 cmt_pytorch/README.md가 원저자 개인 저장소로 링크한다.
CHECKPOINTS = {
    "cmt_s": Checkpoint(
        url="https://github.com/ggjy/CMT.pytorch/releases/download/release-v1/cmt_small.pth",
        filename="cmt_small.pth",
        reported_top1=83.5,
    ),
    "vim_s": Checkpoint(
        url="https://huggingface.co/hustvl/Vim-small-midclstok/resolve/main/vim_s_midclstok_80p5acc.pth",
        filename="vim_s_midclstok_80p5acc.pth",
        reported_top1=80.5,
    ),
}


def sha256_of(path: Path) -> str:
    digest = hashlib.sha256()
    with open(path, "rb") as handle:
        for chunk in iter(lambda: handle.read(1024 * 1024), b""):
            digest.update(chunk)
    return digest.hexdigest()


def _download(url: str, dest: Path) -> None:
    from torch.hub import download_url_to_file

    download_url_to_file(url, str(dest), progress=True)


def fetch(name: str, root: str | Path = "checkpoints") -> Path:
    if name not in CHECKPOINTS:
        raise ValueError(f"알 수 없는 체크포인트 '{name}'")
    root = Path(root)
    root.mkdir(parents=True, exist_ok=True)
    dest = root / CHECKPOINTS[name].filename
    if not dest.exists():
        _download(CHECKPOINTS[name].url, dest)
    return dest


def unwrap_state_dict(obj: dict) -> dict[str, torch.Tensor]:
    """체크포인트가 감싼 실제 가중치를 꺼낸다."""
    for key in ("model", "state_dict"):
        if key in obj and isinstance(obj[key], dict):
            return obj[key]
    if any(isinstance(value, torch.Tensor) for value in obj.values()):
        return obj
    raise ValueError(
        f"state_dict를 찾을 수 없다. 최상위 키: {sorted(obj)[:10]}"
    )
```

- [ ] **Step 4: 통과를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_checkpoints.py -q`
Expected: PASS (5건)

- [ ] **Step 5: 실제로 받고 구조를 확인한다**

Run:
```
wsl bash <래퍼> python -c "
import torch
from models.checkpoints import fetch, sha256_of, unwrap_state_dict
for name in ('cmt_s', 'vim_s'):
    p = fetch(name)
    obj = torch.load(p, map_location='cpu')
    sd = unwrap_state_dict(obj)
    print(name, p.stat().st_size, sha256_of(p)[:16])
    print('  최상위 키:', sorted(obj)[:6] if isinstance(obj, dict) else type(obj))
    print('  파라미터 수:', sum(v.numel() for v in sd.values() if hasattr(v, 'numel')))
    print('  앞 5개:', list(sd)[:5])
"
```
받은 SHA256과 파라미터 수를 SDD 원장에 적는다. CMT는 약 26.3M, Vim은 약 25.8M
근처여야 한다. 크게 벗어나면 Task 3에 들어가기 전에 원인을 밝힌다.

- [ ] **Step 6: 잘못된 출처 기재를 고친다**

`docs/superpowers/specs/2026-08-19-e2-erf-design.md`, `README.md`, `HANDOFF.md`에서
CMT 체크포인트 출처를 `ggjy/CMT.pytorch` 릴리스로 고친다.

- [ ] **Step 7: 커밋**

```bash
git add models/checkpoints.py tests/test_checkpoints.py docs/ README.md HANDOFF.md
git commit -m "feat: fetch the pretrained weights and record what was fetched"
```

---

### Task 3: 세 모델의 가중치 로딩

**Files:**
- Modify: `models/cmt.py`, `models/vim.py`
- Test: `tests/test_pretrained.py`

**Interfaces:**
- Consumes: `models.checkpoints.fetch`, `unwrap_state_dict`
- Produces: `build_model(name, pretrained=True, img_size=224)`가 세 모델 모두에서
  동작. DeiT는 timm이 이미 처리하므로 코드 변경이 없다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_pretrained.py`:

```python
"""가중치가 실제로 들어갔는지 확인한다.

부분 로드된 모델은 예외 없이 조용히 랜덤 모델처럼 행동한다. 그 상태로 ERF를 재면
'학습이 이방성을 만든다'는 결론이 통째로 무의미해진다.
"""
import torch

from data.voc import ensure_voc, load_images, sample_image_paths
from models.registry import MODEL_NAMES, build_model


def _natural_batch(n: int = 4) -> torch.Tensor:
    images = ensure_voc()
    return load_images(sample_image_paths(list(images.glob("*.jpg")), n, seed=0))


def _mean_entropy(model: torch.nn.Module, x: torch.Tensor) -> float:
    with torch.no_grad():
        probs = model(x).softmax(dim=-1)
    return float(-(probs * probs.clamp_min(1e-12).log()).sum(dim=-1).mean())


def test_every_model_loads_pretrained_weights():
    for name in MODEL_NAMES:
        build_model(name, pretrained=True, img_size=224)


def test_pretrained_is_more_confident_than_random_init():
    """엔트로피가 랜덤 초기화와 같으면 가중치가 안 들어간 것이다.

    1000-클래스 균등 분포의 엔트로피는 ln(1000) ≈ 6.91이다. 학습된 모델은 자연
    이미지에서 그보다 훨씬 낮아야 한다.
    """
    x = _natural_batch()
    for name in MODEL_NAMES:
        trained = _mean_entropy(build_model(name, pretrained=True).eval(), x)
        random_init = _mean_entropy(build_model(name, pretrained=False).eval(), x)
        assert trained < random_init - 1.0, f"{name}: {trained:.2f} vs {random_init:.2f}"
```

- [ ] **Step 2: 실패를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_pretrained.py -q`
Expected: FAIL — `NotImplementedError: CMT-S 가중치 로딩은 아직 없다`

- [ ] **Step 3: CMT와 Vim에 로딩을 구현한다**

`models/cmt.py`의 `load_cmt_small`을 교체:

```python
def load_cmt_small(pretrained: bool = False, img_size: int = 224) -> nn.Module:
    model = _cmt_s_official(img_size=img_size)
    if not pretrained:
        return model

    if img_size != 224:
        raise ValueError(
            f"CMT-S 공개 가중치는 224²로 학습됐다. img_size={img_size}는 상대 위치 "
            "bias 보간이 필요하고, 보간한 가중치로 잰 값은 공개 정확도와 대응하지 "
            "않는다. E2는 224²만 쓴다."
        )

    import torch

    from models.checkpoints import fetch, unwrap_state_dict

    state = unwrap_state_dict(torch.load(fetch("cmt_s"), map_location="cpu"))
    model.load_state_dict(state, strict=True)
    return model
```

`models/vim.py`의 `load_vim_small`도 같은 형태로 교체한다(`fetch("vim_s")`,
같은 `img_size` 가드, `strict=True`).

- [ ] **Step 4: 통과를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_pretrained.py -q`
Expected: PASS (2건)

**strict 로드가 키 불일치로 실패하면**: 그것이 이 게이트의 목적이다. `strict=False`로
바꾸지 말 것 — 부분 로드는 조용한 랜덤 모델을 만든다. 대신 실제 키 차이를 출력해
(`set(state) - set(model.state_dict())`) 접두사 차이면 매핑을 코드에 명시하고,
구조 차이면 벤더링한 정의와 체크포인트의 출처 버전이 다른 것이므로 SDD 원장에
적고 보고한다.

- [ ] **Step 5: 회귀 주입으로 테스트를 검증한다**

`strict=True`를 `strict=False`로 바꾸고 로드할 state를 `{}`로 만든 뒤
`tests/test_pretrained.py`를 돌린다. `test_pretrained_is_more_confident_than_random_init`이
실패해야 한다. 확인 후 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add models/cmt.py models/vim.py tests/test_pretrained.py
git commit -m "feat: load the published weights and prove they went in"
```

---

### Task 4: 중심 토큰 프로브 — 이 계획에서 가장 틀리기 쉬운 곳

**Files:**
- Create: `models/probes.py`
- Test: `tests/test_probes.py`

**Interfaces:**
- Consumes: `build_model`
- Produces: `center_token_scalar(model_name: str, model: nn.Module, x: torch.Tensor) -> torch.Tensor`
  — 배치별 스칼라 `(B,)`. 최종 특징맵 중심 토큰의 채널 합.

**함정은 가설이 아니라 확인된 사실이다.** `models/vim_official.py:427`에서 Vim은
cls 토큰을 `token_position = M // 2 = 98`에 **끼워 넣는다**. 그래서 197 토큰
시퀀스의 "가운데"인 인덱스 98은 **패치가 아니라 cls 토큰이다.** 순진하게
`tokens[:, len // 2]`를 집으면 정확히 그 함정에 빠지고, cls 토큰은 전역을 보므로
ERF가 넓고 고르게 나와 **"Vim은 등방"이라는 논문 반증 결론**이 나온다.

올바른 인덱스: 14×14 격자의 중심 `(7, 7)` → 패치 인덱스 `7*14 + 7 = 105` →
cls가 98에 삽입됐으므로 시퀀스 인덱스 `105 + 1 = 106`.

- [ ] **Step 1: 각 모델의 실제 출력 모양을 확인한다**

추측하지 말고 찍어 본다. E1에서 fvcore 연산자 이름을 실측으로 확인한 것과 같은
절차다.

Run:
```
wsl bash <래퍼> python -c "
import torch
from models.registry import build_model
x = torch.zeros(1, 3, 224, 224)
for name in ('deit_s', 'cmt_s', 'vim_s'):
    m = build_model(name, pretrained=False).eval()
    seen = {}
    hooks = [mod.register_forward_hook(
        lambda mod, i, o, n=n: seen.setdefault(n, getattr(o, 'shape', None)))
        for n, mod in m.named_modules()]
    with torch.no_grad():
        m(x)
    for h in hooks: h.remove()
    print('==', name)
    for n, s in list(seen.items())[-6:]:
        print('  ', n, s)
"
```
마지막 정규화 층의 출력 모양을 적어 둔다. DeiT는 `norm` → `(1, 197, 384)`,
Vim은 `norm_f` → `(1, 197, 384)`, CMT는 마지막 stage의 `(1, 49, 512)` 또는
`(1, 512, 7, 7)`이 나올 것이다. **실제로 나온 이름과 모양을 아래 구현에 반영한다.**

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`tests/test_probes.py`:

```python
"""프로브가 cls 토큰이 아니라 중심 패치를 집는지 확인한다.

cls 토큰을 집으면 ERF가 이미지 전체에 퍼져 비등방 지수가 1.0에 수렴한다 —
'Vim은 등방'이라는, 논문을 반증하는 방향의 그럴듯한 오답이다. 아무것도 깨지지
않으므로 결과만 봐서는 알 수 없다.
"""
import torch

from models.probes import center_token_scalar
from models.registry import MODEL_NAMES, build_model

CENTER_PATCH = (112, 112)
CORNER = (8, 8)


def _erf_row(model_name: str) -> torch.Tensor:
    model = build_model(model_name, pretrained=False, img_size=224).eval()
    x = torch.zeros(1, 3, 224, 224, requires_grad=True)
    center_token_scalar(model_name, model, x).sum().backward()
    return x.grad.abs().sum(dim=1)[0]


def test_the_probe_looks_at_the_center_not_everywhere():
    """랜덤 초기화 모델의 ERF는 좁다(Luo et al.). 중심의 gradient가 구석보다
    뚜렷하게 커야 한다. cls 토큰을 집었다면 둘이 비슷해진다."""
    for name in MODEL_NAMES:
        grad = _erf_row(name)
        center = float(grad[CENTER_PATCH[0], CENTER_PATCH[1]])
        corner = float(grad[CORNER[0], CORNER[1]])
        assert center > 10 * corner, f"{name}: 중심 {center:.3e} vs 구석 {corner:.3e}"


def test_the_peak_sits_in_the_middle():
    for name in MODEL_NAMES:
        grad = _erf_row(name)
        row, col = divmod(int(grad.argmax()), grad.shape[1])
        assert abs(row - 112) <= 16 and abs(col - 112) <= 16, f"{name}: ({row}, {col})"


def test_vim_center_index_skips_the_inserted_cls_token():
    """Vim은 cls를 M//2=98에 끼워 넣는다. 시퀀스의 '가운데'가 곧 cls다."""
    from models.probes import vim_center_sequence_index

    assert vim_center_sequence_index(num_patches=196) == 106
```

- [ ] **Step 3: 실패를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_probes.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'models.probes'`

- [ ] **Step 4: 구현한다**

`models/probes.py`. Step 1에서 확인한 실제 모듈 이름과 출력 모양에 맞춘다:

```python
"""최종 특징맵의 중심 토큰을 집는다.

이 파일이 틀리면 E2 전체가 조용히 틀린다. cls 토큰을 집으면 ERF가 전역으로 퍼져
'완벽한 등방'이라는 그럴듯한 오답이 나오고, 예외도 경고도 나지 않는다.
"""
import torch
import torch.nn as nn

# 어느 모듈의 출력을 최종 특징맵으로 볼지. Step 1에서 실측으로 확인한 이름이다.
FEATURE_MODULE = {
    "deit_s": "norm",
    "vim_s": "norm_f",
    "cmt_s": "norm",
}


def vim_center_sequence_index(num_patches: int) -> int:
    """Vim의 시퀀스에서 중심 패치가 있는 위치.

    Vim은 cls 토큰을 M//2에 '끼워 넣는다'(vim_official.py:427). 중심 패치는
    격자 (g//2, g//2)이고, 그 인덱스가 삽입 위치보다 뒤이므로 1칸 밀린다.
    """
    grid = int(num_patches ** 0.5)
    patch_index = (grid // 2) * grid + (grid // 2)
    cls_position = num_patches // 2
    return patch_index + 1 if patch_index >= cls_position else patch_index


def _capture(model: nn.Module, module_name: str, x: torch.Tensor) -> torch.Tensor:
    captured = {}
    module = dict(model.named_modules())[module_name]
    handle = module.register_forward_hook(
        lambda _module, _inputs, output: captured.setdefault("out", output)
    )
    try:
        model(x)
    finally:
        handle.remove()
    if "out" not in captured:
        raise RuntimeError(f"'{module_name}'이 forward에서 불리지 않았다")
    return captured["out"]


def center_token_scalar(
    model_name: str, model: nn.Module, x: torch.Tensor
) -> torch.Tensor:
    features = _capture(model, FEATURE_MODULE[model_name], x)

    if features.dim() == 4:  # (B, C, H, W)
        _, _, height, width = features.shape
        return features[:, :, height // 2, width // 2].sum(dim=1)

    batch, tokens, _ = features.shape
    if model_name == "vim_s":
        index = vim_center_sequence_index(num_patches=tokens - 1)
    elif model_name == "deit_s":
        grid = int((tokens - 1) ** 0.5)
        index = 1 + (grid // 2) * grid + (grid // 2)  # cls가 맨 앞
    else:
        grid = int(tokens ** 0.5)
        index = (grid // 2) * grid + (grid // 2)
    return features[:, index, :].sum(dim=1)
```

- [ ] **Step 5: 통과를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_probes.py -q`
Expected: PASS (3건)

- [ ] **Step 6: 회귀 주입 — 이 태스크에서 가장 중요한 단계**

`vim_center_sequence_index`가 항상 `num_patches // 2`(= cls 토큰 위치)를 돌려주도록
바꾸고 `tests/test_probes.py`를 돌린다. `test_the_probe_looks_at_the_center_not_everywhere`와
`test_vim_center_index_skips_the_inserted_cls_token`이 실패해야 한다. 실패하지 않으면
테스트가 이름값을 못 하는 것이므로, 되돌리기 전에 테스트를 고친다.

- [ ] **Step 7: 커밋**

```bash
git add models/probes.py tests/test_probes.py
git commit -m "feat: probe the center patch, not the class token"
```

---

### Task 5: ERF map 누적

**Files:**
- Create: `bench/erf.py`
- Test: `tests/test_erf.py`

**Interfaces:**
- Consumes: `models.probes.center_token_scalar`
- Produces: `accumulate_erf(model_name, model, images, device="cuda") -> numpy.ndarray`
  — `(224, 224)` float64, 이미지별 최댓값 정규화 후 평균

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_erf.py`:

```python
import numpy as np
import torch
import torch.nn as nn

from bench.erf import accumulate_erf


class CenterPixel(nn.Module):
    """중심 픽셀만 보는 모델. ERF가 정확히 한 점이어야 한다."""

    def forward(self, x):
        return x[:, :, 112, 112].sum(dim=1)


def test_a_center_only_model_gives_a_single_bright_pixel(monkeypatch):
    monkeypatch.setattr(
        "bench.erf.center_token_scalar", lambda name, model, x: model(x)
    )
    images = torch.zeros(3, 3, 224, 224)

    erf = accumulate_erf("toy", CenterPixel(), images, device="cpu")

    assert erf.shape == (224, 224)
    assert np.unravel_index(erf.argmax(), erf.shape) == (112, 112)
    assert erf.sum() == erf[112, 112]


def test_one_loud_image_does_not_dominate_the_average(monkeypatch):
    """이미지별 정규화가 빠지면 gradient가 큰 한 장이 평균을 삼킨다."""
    scales = iter([1.0, 1000.0])
    monkeypatch.setattr(
        "bench.erf.center_token_scalar",
        lambda name, model, x: next(scales) * model(x),
    )
    erf = accumulate_erf("toy", CenterPixel(), torch.zeros(2, 3, 224, 224), device="cpu")

    assert erf[112, 112] == 1.0


def test_the_input_batch_is_not_modified(monkeypatch):
    """requires_grad를 원본에 걸면 호출한 쪽의 텐서가 오염된다."""
    monkeypatch.setattr(
        "bench.erf.center_token_scalar", lambda name, model, x: model(x)
    )
    images = torch.zeros(1, 3, 224, 224)
    accumulate_erf("toy", CenterPixel(), images, device="cpu")

    assert images.requires_grad is False
    assert images.grad is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_erf.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'bench.erf'`

- [ ] **Step 3: 구현한다**

`bench/erf.py`:

```python
"""Luo et al.(2016) 방식의 ERF 측정.

한 장씩 backward를 돌린다. 배치로 묶으면 중심 토큰 스칼라의 합에 대한 gradient가
되어 이미지별 정규화를 할 수 없고, 정규화가 없으면 gradient가 큰 한 장이 평균을
삼킨다.
"""
import numpy as np
import torch
import torch.nn as nn

from models.probes import center_token_scalar


def accumulate_erf(
    model_name: str,
    model: nn.Module,
    images: torch.Tensor,
    device: str = "cuda",
) -> np.ndarray:
    model = model.to(device).eval()
    total = np.zeros(images.shape[-2:], dtype=np.float64)

    for image in images:
        x = image.unsqueeze(0).to(device).clone().requires_grad_(True)
        center_token_scalar(model_name, model, x).sum().backward()
        grad = x.grad.detach().abs().sum(dim=1)[0].cpu().numpy().astype(np.float64)
        peak = grad.max()
        if peak > 0:
            grad = grad / peak
        total += grad

    return total / len(images)
```

- [ ] **Step 4: 통과를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_erf.py -q`
Expected: PASS (3건)

- [ ] **Step 5: 회귀 주입**

`grad = grad / peak` 줄을 지우고 `test_one_loud_image_does_not_dominate_the_average`가
실패하는지 확인한 뒤 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add bench/erf.py tests/test_erf.py
git commit -m "feat: average the receptive field one image at a time"
```

---

### Task 6: 세 지표와 수렴 판정

**Files:**
- Modify: `bench/erf.py`
- Test: `tests/test_erf_metrics.py`

**Interfaces:**
- Consumes: 없음 (순수 함수)
- Produces:
  - `anisotropy_index(erf: np.ndarray) -> float`
  - `principal_angle_deg(erf: np.ndarray) -> float`
  - `decay_ratio(erf: np.ndarray, max_distance: int = 64) -> float`
  - `has_converged(values: list[float], tolerance: float = 0.05) -> bool`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_erf_metrics.py`:

```python
import numpy as np
import pytest

from bench.erf import anisotropy_index, decay_ratio, has_converged, principal_angle_deg


def _gaussian(sigma_x: float, sigma_y: float, size: int = 224) -> np.ndarray:
    axis = np.arange(size) - size // 2
    x, y = np.meshgrid(axis, axis)  # x는 열(수평), y는 행(수직)
    return np.exp(-(x**2) / (2 * sigma_x**2) - (y**2) / (2 * sigma_y**2))


def test_a_round_blob_is_isotropic():
    assert anisotropy_index(_gaussian(20, 20)) == pytest.approx(1.0, abs=0.02)


def test_a_stretched_blob_reports_its_stretch():
    """수평으로 3배 늘인 가우시안의 비등방 지수는 3이다."""
    assert anisotropy_index(_gaussian(30, 10)) == pytest.approx(3.0, rel=0.05)


def test_the_principal_axis_of_a_horizontal_blob_is_horizontal():
    assert principal_angle_deg(_gaussian(30, 10)) == pytest.approx(0.0, abs=2.0)


def test_the_principal_axis_of_a_vertical_blob_is_vertical():
    assert principal_angle_deg(_gaussian(10, 30)) == pytest.approx(90.0, abs=2.0)


def test_decay_ratio_is_one_when_both_axes_fall_alike():
    assert decay_ratio(_gaussian(20, 20)) == pytest.approx(1.0, rel=0.05)


def test_decay_ratio_exceeds_one_when_the_vertical_falls_faster():
    """논문 3.1절이 Vim에 대해 예측하는 방향이다 — 수직이 더 가파르다."""
    assert decay_ratio(_gaussian(30, 10)) > 1.5


def test_convergence_looks_at_the_last_two_points():
    assert has_converged([5.0, 2.0, 1.30, 1.31]) is True
    assert has_converged([5.0, 2.0, 1.30, 1.60]) is False


def test_a_single_measurement_has_not_converged():
    """점이 하나면 변화를 볼 수 없다. True를 돌려주면 N=16짜리 값이 수렴한
    값으로 논문에 실린다."""
    assert has_converged([1.3]) is False
```

- [ ] **Step 2: 실패를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_erf_metrics.py -q`
Expected: FAIL — `ImportError: cannot import name 'anisotropy_index'`

- [ ] **Step 3: 구현한다**

`bench/erf.py`에 추가:

```python
def _covariance(erf: np.ndarray) -> np.ndarray:
    """ERF를 합이 1인 2D 분포로 보고 2차 모먼트 공분산을 구한다."""
    weights = erf / erf.sum()
    rows, cols = np.indices(erf.shape)
    mean_row = float((weights * rows).sum())
    mean_col = float((weights * cols).sum())
    d_row = rows - mean_row
    d_col = cols - mean_col
    return np.array([
        [(weights * d_col * d_col).sum(), (weights * d_col * d_row).sum()],
        [(weights * d_col * d_row).sum(), (weights * d_row * d_row).sum()],
    ])


def anisotropy_index(erf: np.ndarray) -> float:
    """√(λ_max / λ_min). 등방이면 1.0."""
    eigenvalues = np.linalg.eigvalsh(_covariance(erf))
    return float(np.sqrt(eigenvalues.max() / eigenvalues.min()))


def principal_angle_deg(erf: np.ndarray) -> float:
    """주축과 수평축 사이 각도, [0, 90]. 지수가 1에 가까우면 의미가 없다."""
    values, vectors = np.linalg.eigh(_covariance(erf))
    x, y = vectors[:, int(values.argmax())]
    return float(np.degrees(np.arctan2(abs(y), abs(x))))


def _log_slope(profile: np.ndarray) -> float:
    positive = profile > 0
    distances = np.arange(1, len(profile) + 1)[positive]
    return float(np.polyfit(distances, np.log(profile[positive]), 1)[0])


def decay_ratio(erf: np.ndarray, max_distance: int = 64) -> float:
    """수직 감쇠 기울기 / 수평 감쇠 기울기. 1보다 크면 수직이 더 가파르다."""
    row, col = np.unravel_index(erf.argmax(), erf.shape)
    offsets = np.arange(1, max_distance + 1)
    horizontal = (erf[row, col + offsets] + erf[row, col - offsets]) / 2
    vertical = (erf[row + offsets, col] + erf[row - offsets, col]) / 2
    return float(_log_slope(vertical) / _log_slope(horizontal))


def has_converged(values: list[float], tolerance: float = 0.05) -> bool:
    """마지막 두 점의 상대 변화가 tolerance 이내인가.

    점이 하나뿐이면 판단할 근거가 없으므로 False다. True를 돌려주면 N=16으로 잰
    값이 '수렴한 값'으로 논문에 실린다.
    """
    if len(values) < 2:
        return False
    previous, latest = values[-2], values[-1]
    if previous == 0:
        return latest == 0
    return abs(latest - previous) / abs(previous) <= tolerance
```

`decay_ratio`가 `col + offsets`로 224 경계를 넘지 않으려면 피크가 중심 근처여야
한다. Task 4의 프로브 테스트가 그것을 이미 보장한다(피크가 중심 ±16 이내,
`112 + 16 + 64 = 192 < 224`).

- [ ] **Step 4: 통과를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_erf_metrics.py -q`
Expected: PASS (8건)

- [ ] **Step 5: 회귀 주입**

`has_converged`의 `len(values) < 2` 가드를 지우고 `[1.3]`에서 True가 나오게 만든 뒤
`test_a_single_measurement_has_not_converged`가 실패하는지 확인하고 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add bench/erf.py tests/test_erf_metrics.py
git commit -m "feat: turn anisotropy from an adjective into three numbers"
```

---

### Task 7: 실험 오케스트레이션

**Files:**
- Create: `experiments/e2_erf.py`
- Modify: `.gitignore`
- Test: `tests/test_e2.py`

**Interfaces:**
- Consumes: Task 1~6 전부
- Produces: `run_erf(model_names, conditions, sample_sizes, out_dir) -> pandas.DataFrame`

**조건 셋** (spec의 "조건 세 가지"):

| 조건 이름 | 가중치 | 입력 |
|-----------|--------|------|
| `natural` | 사전학습 | VOC |
| `noise` | 사전학습 | `torch.randn` |
| `random_init` | 랜덤 초기화 | VOC |

**CSV 열**: `model, condition, n_images, anisotropy, principal_angle_deg,
decay_ratio, converged, status, error`

사용한 이미지 목록도 이 태스크에서 남긴다. 어떤 이미지로 잰 값인지 없으면 재현이
불가능하다. 이름을 만드는 일은 모듈 수준 함수 `_image_names(n)`로 뺀다 — `run_erf`
안에서 `ensure_voc()`를 직접 부르면 단위 테스트가 VOC 2GB를 받으러 간다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_e2.py`:

```python
import json

import numpy as np
import pandas as pd

import experiments.e2_erf as e2
from experiments.e2_erf import COLUMNS, CONDITIONS, SAMPLE_SIZES, run_erf


def _stub_erf(model_name, model, images, device="cuda"):
    axis = np.arange(224) - 112
    x, y = np.meshgrid(axis, axis)
    return np.exp(-(x**2) / (2 * 30**2) - (y**2) / (2 * 10**2))


def _offline(monkeypatch):
    """단위 테스트가 네트워크를 건드리지 않게 막는다.

    이걸 빼면 체크포인트 500MB와 VOC 2GB를 테스트마다 받으러 간다. 느린 게
    문제가 아니라, 네트워크가 없는 곳에서 테스트가 실패해 '코드가 깨졌다'로
    읽힌다.
    """
    import torch

    monkeypatch.setattr(e2, "accumulate_erf", _stub_erf)
    monkeypatch.setattr(e2, "_images_for", lambda *a, **k: torch.zeros(4, 3, 224, 224))
    monkeypatch.setattr(e2, "_image_names", lambda n: [f"img_{i}.jpg" for i in range(n)])
    monkeypatch.setattr(e2, "build_model", lambda *a, **k: torch.nn.Identity())
    monkeypatch.setattr(e2, "_checkpoint_hashes", lambda: {})


def test_the_three_conditions_are_declared():
    assert set(CONDITIONS) == {"natural", "noise", "random_init"}


def test_one_row_per_model_condition_and_sample_size(tmp_path, monkeypatch):
    _offline(monkeypatch)

    df = run_erf(model_names=("deit_s",), sample_sizes=(2, 4), out_dir=tmp_path)

    assert len(df) == 1 * 3 * 2
    assert list(df.columns) == COLUMNS


def test_the_erf_maps_are_saved_next_to_the_metrics(tmp_path, monkeypatch):
    """그림은 커밋된 원시 데이터에서 나와야 한다. 지표만 남기면 heatmap을 다시
    그릴 수 없다."""
    _offline(monkeypatch)

    run_erf(model_names=("deit_s",), sample_sizes=(4,), out_dir=tmp_path)

    maps = np.load(tmp_path / "erf_maps.npz")
    assert "deit_s__natural" in maps
    assert maps["deit_s__natural"].shape == (224, 224)


def test_env_json_records_the_checkpoint_hashes(tmp_path, monkeypatch):
    """어느 가중치로 잰 값인지 없으면 나중에 받은 다른 체크포인트의 결과와
    구분할 수 없다."""
    _offline(monkeypatch)
    monkeypatch.setattr(e2, "_checkpoint_hashes", lambda: {"cmt_s": "abc123"})

    run_erf(model_names=("deit_s",), sample_sizes=(4,), out_dir=tmp_path)

    env = json.loads((tmp_path / "env.json").read_text())
    assert env["checkpoints"] == {"cmt_s": "abc123"}


def test_the_image_list_is_recorded(tmp_path, monkeypatch):
    """어떤 이미지로 잰 값인지 없으면 재현이 불가능하다."""
    _offline(monkeypatch)

    run_erf(model_names=("deit_s",), sample_sizes=(4,), out_dir=tmp_path)

    assert (tmp_path / "images.txt").read_text().splitlines() == [
        "img_0.jpg", "img_1.jpg", "img_2.jpg", "img_3.jpg"
    ]


def test_a_failing_cell_does_not_lose_the_rest(tmp_path, monkeypatch):
    calls = {"n": 0}

    def flaky(*args, **kwargs):
        calls["n"] += 1
        if calls["n"] == 2:
            raise RuntimeError("이 조건만 터진다")
        return _stub_erf(*args, **kwargs)

    _offline(monkeypatch)
    monkeypatch.setattr(e2, "accumulate_erf", flaky)

    df = run_erf(model_names=("deit_s",), sample_sizes=(4,), out_dir=tmp_path)

    assert len(df) == 3
    assert list(df["status"]).count("error") == 1
    assert pd.read_csv(tmp_path / "erf_metrics.csv").shape[0] == 3
```

- [ ] **Step 2: 실패를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_e2.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'experiments.e2_erf'`

- [ ] **Step 3: 구현한다**

`experiments/e2_erf.py`:

```python
"""E2 — ERF 정량 측정.

논문 3.1절은 "CMT는 등방, Vim은 scan 방향 이방"을 그림 없이 서술로만 편다.
이 스크립트가 그 서술을 대체할 숫자를 만든다.
"""
import json
from pathlib import Path

import numpy as np
import pandas as pd
import torch

from bench.env import snapshot
from bench.erf import (
    accumulate_erf,
    anisotropy_index,
    decay_ratio,
    has_converged,
    principal_angle_deg,
)
from data.voc import ensure_voc, load_images, sample_image_paths
from models.registry import MODEL_NAMES, build_model

CONDITIONS = ("natural", "noise", "random_init")
SAMPLE_SIZES = (16, 32, 64, 128, 256)
SEED = 0

COLUMNS = [
    "model",
    "condition",
    "n_images",
    "anisotropy",
    "principal_angle_deg",
    "decay_ratio",
    "converged",
    "status",
    "error",
]


def _images_for(condition: str, n: int) -> torch.Tensor:
    if condition == "noise":
        return torch.randn(n, 3, 224, 224, generator=torch.Generator().manual_seed(SEED))
    paths = sample_image_paths(sorted(ensure_voc().glob("*.jpg")), n, seed=SEED)
    return load_images(paths)


def _checkpoint_hashes() -> dict[str, str]:
    from models.checkpoints import CHECKPOINTS, fetch, sha256_of

    return {name: sha256_of(fetch(name)) for name in CHECKPOINTS}


def _image_names(n: int) -> list[str]:
    """실제로 쓴 이미지 파일 이름. results/e2/images.txt에 들어간다."""
    paths = sample_image_paths(sorted(ensure_voc().glob("*.jpg")), n, seed=SEED)
    return [path.name for path in paths]


def run_erf(
    model_names: tuple[str, ...] = MODEL_NAMES,
    sample_sizes: tuple[int, ...] = SAMPLE_SIZES,
    out_dir: Path | str = "results/e2",
) -> pd.DataFrame:
    out_dir = Path(out_dir)
    out_dir.mkdir(parents=True, exist_ok=True)
    device = "cuda" if torch.cuda.is_available() else "cpu"

    env = snapshot()
    env["checkpoints"] = _checkpoint_hashes()
    env["seed"] = SEED
    (out_dir / "env.json").write_text(json.dumps(env, indent=2))
    (out_dir / "images.txt").write_text("
".join(_image_names(max(sample_sizes))))

    rows: list[dict] = []
    maps: dict[str, np.ndarray] = {}

    for name in model_names:
        for condition in CONDITIONS:
            history: list[float] = []
            model = build_model(name, pretrained=condition != "random_init")
            for n in sample_sizes:
                row = {column: None for column in COLUMNS}
                row.update(model=name, condition=condition, n_images=n, status="ok")
                try:
                    erf = accumulate_erf(
                        name, model, _images_for(condition, n), device=device
                    )
                except Exception as exc:  # 한 조건의 실패로 전체를 잃지 않는다
                    row.update(status="error", error=f"{type(exc).__name__}: {exc}")
                    rows.append(row)
                    continue

                history.append(anisotropy_index(erf))
                row.update(
                    anisotropy=history[-1],
                    principal_angle_deg=principal_angle_deg(erf),
                    decay_ratio=decay_ratio(erf),
                    converged=has_converged(history),
                )
                rows.append(row)
                maps[f"{name}__{condition}"] = erf

            pd.DataFrame(rows, columns=COLUMNS).to_csv(
                out_dir / "erf_metrics.csv", index=False
            )
            np.savez_compressed(out_dir / "erf_maps.npz", **maps)

    return pd.DataFrame(rows, columns=COLUMNS)


if __name__ == "__main__":
    print(run_erf().to_string(index=False))
```

- [ ] **Step 4: `.gitignore`에 예외를 넣는다**

`results/**/*.npy`·`results/**/*.npz` 규칙 아래에 추가한다:

```
# ERF map은 중간 덤프가 아니라 E2의 1차 측정값이다. 그림이 커밋된 데이터에서
# 나와야 한다는 원칙 때문에 이것만 예외로 추적한다(약 1.2MB).
!results/e2/erf_maps.npz
```

- [ ] **Step 5: 통과를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_e2.py -q`
Expected: PASS (6건)

- [ ] **Step 6: 커밋**

```bash
git add experiments/e2_erf.py tests/test_e2.py .gitignore
git commit -m "feat: run the ERF sweep and keep the maps the figure needs"
```

---

### Task 8: 그림

**Files:**
- Create: `figures/e2_plot.py`
- Test: `tests/test_e2_plot.py`

**Interfaces:**
- Consumes: `results/e2/erf_metrics.csv`, `results/e2/erf_maps.npz`
- Produces: `plot_erf(csv_path, npz_path, out_path) -> Path`,
  `final_metrics(df) -> pandas.DataFrame` (순수 함수)

`figures/e1_plot.py`와 같은 구조다 — 무엇을 그릴지 정하는 판단을 순수 함수로 빼서
matplotlib을 거치지 않고 검증한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/test_e2_plot.py`:

```python
import numpy as np
import pandas as pd
import pytest

from figures.e2_plot import final_metrics, plot_erf

BASE = {
    "model": "vim_s",
    "condition": "natural",
    "n_images": 256,
    "anisotropy": 1.42,
    "principal_angle_deg": 3.1,
    "decay_ratio": 1.8,
    "converged": True,
    "status": "ok",
    "error": None,
}


def _row(**overrides):
    row = dict(BASE)
    row.update(overrides)
    return row


def test_final_metrics_takes_the_largest_sample_size():
    df = pd.DataFrame([_row(n_images=16, anisotropy=2.9), _row(n_images=256)])

    final = final_metrics(df)

    assert len(final) == 1
    assert final.iloc[0]["anisotropy"] == pytest.approx(1.42)


def test_unconverged_rows_are_kept_and_flagged():
    """수렴하지 않은 값을 버리면 '왜 빈칸인지 알 수 없는 표'가 된다. 남기되
    수렴 여부를 함께 싣는다."""
    df = pd.DataFrame([_row(converged=False)])

    final = final_metrics(df)

    assert len(final) == 1, "수렴하지 않은 행이 표에서 사라졌다"
    assert bool(final.iloc[0]["converged"]) is False


def test_failed_rows_never_reach_the_table():
    df = pd.DataFrame([_row(status="error", anisotropy=None)])

    assert final_metrics(df).empty


def test_writes_a_png(tmp_path):
    csv = tmp_path / "erf_metrics.csv"
    pd.DataFrame([_row(model=m, condition=c)
                  for m in ("deit_s", "cmt_s", "vim_s")
                  for c in ("natural", "noise", "random_init")]).to_csv(csv, index=False)
    npz = tmp_path / "erf_maps.npz"
    np.savez_compressed(npz, **{
        f"{m}__{c}": np.random.rand(224, 224)
        for m in ("deit_s", "cmt_s", "vim_s")
        for c in ("natural", "noise", "random_init")
    })

    out = plot_erf(csv, npz, tmp_path / "e2.png")

    assert out.exists() and out.stat().st_size > 0
```

- [ ] **Step 2: 실패를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_e2_plot.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'figures.e2_plot'`

- [ ] **Step 3: 구현한다**

`figures/e2_plot.py`. 그림은 3행(모델) × 4열 — `natural`·`noise`·`random_init`
heatmap 셋과 비등방 지수의 수렴 곡선 하나.

```python
"""E2 그림. results/e2/ 만 읽는다.

무엇을 표에 올릴지 정하는 판단은 `final_metrics`에 있다. 순수 함수라 matplotlib을
거치지 않고 직접 검증할 수 있다.
"""
from pathlib import Path

import matplotlib

matplotlib.use("Agg")
import matplotlib.pyplot as plt
import numpy as np
import pandas as pd

CONDITIONS = ("natural", "noise", "random_init")


def final_metrics(df: pd.DataFrame) -> pd.DataFrame:
    """모델·조건마다 가장 큰 N의 행. 실패한 행은 표에 올리지 않는다."""
    usable = df[df["status"] == "ok"]
    if usable.empty:
        return usable
    latest = usable.sort_values("n_images").groupby(["model", "condition"]).tail(1)
    return latest.sort_values(["model", "condition"]).reset_index(drop=True)


def plot_erf(csv_path: Path | str, npz_path: Path | str, out_path: Path | str) -> Path:
    df = pd.read_csv(csv_path)
    if df.empty:
        raise ValueError(f"{csv_path}가 비어 있다 — 먼저 실험을 실행할 것")
    maps = np.load(npz_path)

    models = list(dict.fromkeys(final_metrics(df)["model"]))
    fig, axes = plt.subplots(len(models), 4, figsize=(18, 4 * len(models)),
                             squeeze=False)

    for row, model in enumerate(models):
        for column, condition in enumerate(CONDITIONS):
            ax = axes[row][column]
            key = f"{model}__{condition}"
            if key in maps:
                ax.imshow(maps[key], cmap="viridis")
                metric = final_metrics(df).query(
                    "model == @model and condition == @condition"
                )
                if not metric.empty:
                    ax.set_title(
                        f"{model} / {condition}\n"
                        f"anisotropy {metric.iloc[0]['anisotropy']:.2f}, "
                        f"decay {metric.iloc[0]['decay_ratio']:.2f}"
                    )
            else:
                ax.text(0.5, 0.5, "not measured", transform=ax.transAxes,
                        ha="center", va="center")
            ax.set_xticks([])
            ax.set_yticks([])

        curve = df[(df["model"] == model) & (df["condition"] == "natural")
                   & (df["status"] == "ok")].sort_values("n_images")
        ax = axes[row][3]
        ax.plot(curve["n_images"], curve["anisotropy"], marker="o")
        ax.set_xscale("log", base=2)
        ax.set_xlabel("images averaged")
        ax.set_ylabel("anisotropy index")
        ax.set_title(f"{model} / convergence")

    fig.tight_layout()
    fig.savefig(out_path, dpi=200)
    plt.close(fig)
    return Path(out_path)


if __name__ == "__main__":
    print(plot_erf("results/e2/erf_metrics.csv", "results/e2/erf_maps.npz",
                   "results/e2/e2_erf.png"))
```

- [ ] **Step 4: 통과를 확인한다**

Run: `wsl bash <래퍼> python -m pytest tests/test_e2_plot.py -q`
Expected: PASS (4건)

- [ ] **Step 5: 커밋**

```bash
git add figures/e2_plot.py tests/test_e2_plot.py
git commit -m "feat: show the receptive fields next to the numbers"
```

---

### Task 9: 고정 환경 실측과 정직성 검사

**Files:**
- Create: `results/e2/erf_metrics.csv`, `results/e2/erf_maps.npz`,
  `results/e2/env.json`, `results/e2/e2_erf.png`, `results/e2/images.txt`
- Modify: `experiments/e2_erf.py` (사용한 이미지 목록 기록)

- [ ] **Step 1: 전체 테스트를 고정 환경에서 돌린다**

Run: `wsl bash <래퍼> python -m pytest -q`
Expected: 기존 97건 + 이 계획의 신규 테스트가 전부 통과. 실패가 있으면 실측에
들어가지 않는다.

- [ ] **Step 2: 실측을 돌린다**

Run: `wsl bash <래퍼> python -u -m experiments.e2_erf`

3모델 × 3조건 × 5개 N. 가장 큰 셀이 256장 forward+backward이므로 모델당 몇 분이면
끝난다. E1의 sweep보다 훨씬 가볍다.

- [ ] **Step 3: 정직성 검사 — 결과를 믿기 전에 통과해야 한다**

```
wsl bash <래퍼> python -c "
import json, numpy as np, pandas as pd
df = pd.read_csv('results/e2/erf_metrics.csv')
print('행/상태:', len(df), df['status'].value_counts().to_dict())

final = df.sort_values('n_images').groupby(['model','condition']).tail(1)
print(final[['model','condition','anisotropy','principal_angle_deg','decay_ratio','converged']].to_string(index=False))

print('수렴 실패:', list(final[~final['converged'].astype(bool)][['model','condition']].itertuples(index=False)))

maps = np.load('results/e2/erf_maps.npz')
for k in maps.files:
    m = maps[k]
    r, c = np.unravel_index(m.argmax(), m.shape)
    print(k, '피크', (r, c), '중심질량반경', float(np.sqrt(((m/m.sum())*((np.indices(m.shape)[0]-112)**2+(np.indices(m.shape)[1]-112)**2)).sum())))
print(json.load(open('results/e2/env.json'))['checkpoints'])
"
```

통과 기준:

1. **`status`가 전부 `ok`.** `status`는 `accumulate_erf`가 원본 ERF 맵을 얻었는지
   만 본다 — 지표(비등방 지수·주축 각도·감쇠비) 각각은 맵을 얻은 뒤 독립적으로
   계산되고, 하나가 정의되지 않아도(예: 감쇠비가 피크 경계 근접으로 계산 불가)
   그 지표 열만 비고 `status`는 `ok`로 남으며 사유가 `error` 열에 남는다. 이
   기준이 실제로 잡는 것은 "원본조차 못 얻은 셀"이고, 지표 하나가 정의되지
   않는 셀은 위반이 아니다. (초판은 지표 계산 네 개를 한 블록에 묶어, 감쇠비
   하나의 실패로 이미 성공한 맵까지 포함해 셀 전체가 `status="error"`로 사라지고
   그림이 실제로 측정된 셀을 "not measured"로 그렸다. `status`가 전부 `ok`라고
   서술로 통과시켜 놓고 이 기준 자체는 한 번도 재정의된 적이 없다는 게 리뷰
   지적이었다 — 이 항목이 그 개정이다.)
2. **`natural`·`random_init` 맵의 피크가 중심 ±16 이내.** 벗어나면 프로브가 틀린
   것이다. **`noise` 맵은 이 기준에서 제외한다** — `noise`는 사전학습 가중치에
   학습 분포 밖 입력(`torch.randn`)을 먹이는 조건이라, 피크가 어디로 튀어도 그
   자체가 프로브 결함의 증거는 아니다(같은 모델의 `natural`·`random_init`이
   멀쩡하다면 인덱스 매핑은 무죄다 — 가드 1이 그 경로를 이미 막는다). Task 9
   실측에서 정확히 이 패턴이 나왔다: `cmt_s`가 `noise`에서만 5개 표본 크기 전부에
   걸쳐 피크가 이미지 모서리(중심에서 ~110px)로 튀었고, `natural`·`random_init`은
   전부 중심 ±16 이내였다. `noise`의 피크 이탈은 게이트 실패가 아니라 그 자체로
   기록할 결과다 — 위치(피크 좌표, 중심으로부터 거리)를 결과에 남긴다.
3. **각 모델에서 `random_init`의 질량 반경이 `natural`보다 작다.** 이것이 cls
   토큰 오인을 잡는 게이트다. 크거나 같으면 결과를 쓰지 말고 Task 4로 돌아간다.
   `noise` 조건은 이 비교에 관여하지 않으므로 위 기준 2의 범위 축소와 무관하게
   그대로 유지한다. `random_init`은 `build_model(..., pretrained=False)` 직전에
   고정 시드를 걸어 매 실행 같은 무작위 초기화를 재현한다 — 시드 없이는 이
   숫자 자체가 실행마다 달라져 게이트를 재현할 수 없다.
4. **세 지표(비등방 지수·주축 각도·감쇠비) 각각 독립적으로 수렴을 본다.**
   마지막 두 점 상대 변화가 5%를 넘는 지표는 그 값을 논문에 인용하지 않는다.
   `converged` 단일 열로 뭉뚱그리면 anisotropy_index만 수렴하고 decay_ratio는
   전혀 수렴하지 않은 셀이 "수렴함"으로 찍힌다 — 실제로 `natural` 조건의
   `vim_s`(+7.5%)와 `cmt_s`(+15.1%)에서 감쇠비가 계획의 5% 기준을 넘겼는데도
   `anisotropy_index` 이력만 보던 통짜 `converged` 열은 `True`를 찍었다.
   `SAMPLE_SIZES`에 512를 더해 다시 돌린다.

**게이트가 실패하면 게이트를 고치지 말고 원인을 찾는다.** E1에서 "미등록 연산
0개"라는 달성 불가능한 기준을 세웠다가 가드를 푸는 방향으로 갈 뻔한 전례가 있다.
여기서의 게이트는 전부 비교 기준이라 달성 가능하다. 기준 2의 위 범위 축소는
게이트를 통과시키려고 기준을 푼 것이 아니라, `noise` 조건이 애초에 이 게이트의
설계 의도(프로브 인덱싱 검증) 대상이 아니었다는 사실을 뒤늦게 반영한 것이다 —
`natural`에 대한 기준 2, `natural` vs `random_init`에 대한 기준 3은 전혀 완화되지
않았다. 기준 1의 위 개정도 마찬가지다 — 완화가 아니라 "지표 하나가 정의되지
않는 것"과 "원본을 못 얻는 것"이 애초에 다른 심각도였다는 사실을 반영한다.

- [ ] **Step 4: 사전 등록 예측과 대조한다**

spec의 예측 표와 실측을 나란히 놓고 SDD 원장에 적는다. **예측이 빗나갔으면 예측을
고치지 말고 빗나갔다고 적는다.** 특히 Vim의 비등방 지수가 1.2 미만이면 논문
3.1절을 실측에 맞춰 고쳐야 하고, 그것이 이 저장소의 목적이다.

- [ ] **Step 5: 그림을 만든다**

Run: `wsl bash <래퍼> python -m figures.e2_plot`

- [ ] **Step 6: 커밋**

```bash
git add results/e2/ experiments/e2_erf.py tests/test_e2.py
git commit -m "measure: record what the receptive fields actually look like"
```

- [ ] **Step 7: HANDOFF.md를 갱신한다**

E2 완료, 실측 지표, 사전 등록 예측과의 대조 결과, 논문 3.1절에 반영할 문장을 적는다.

```bash
git add HANDOFF.md
git commit -m "docs: bring the handoff up to the E2 result"
```

---

## Self-Review

**Spec 커버리지**

| spec 요구 | 태스크 |
|-----------|--------|
| 사전 등록 예측 | Task 9 Step 5에서 대조 |
| VOC 고정 샘플, 224² center crop, ImageNet 정규화 | Task 1 |
| 중심 토큰 채널 합, `(H//2, W//2)` 고정 | Task 4 |
| 이미지별 정규화 후 누적 평균 | Task 5 |
| 조건 세 가지 | Task 7 |
| 세 지표 | Task 6 |
| 수렴 곡선, 5% 기준 | Task 6 (`has_converged`), Task 7 (`SAMPLE_SIZES`) |
| 정직성 장치 1 — 인덱스 매핑 테스트 | Task 4 |
| 정직성 장치 2 — 랜덤 초기화 비교 게이트 | Task 4 Step 2, Task 9 Step 4 기준 3 |
| 정직성 장치 3 — strict 로드 + 엔트로피 | Task 3 |
| 체크포인트 SHA256 기록 | Task 2, Task 7 |
| `images.txt` | Task 7 |
| `.gitignore` 예외 | Task 7 Step 4 |
| 산출물 5종 | Task 7·8·9 |
| 회귀 주입 확인 | Task 3·4·5·6 각 마지막 |

**해소한 문제**: spec은 CMT 체크포인트 출처를 huawei-noah/Efficient-AI-Backbones
릴리스로 적었으나 그곳에 자산이 없다. 실제 배포처(`ggjy/CMT.pytorch`)를 Task 2에
넣고, 같은 태스크에서 spec·README·HANDOFF를 고치는 단계를 뒀다.

**범위 밖으로 남긴 것**: 단계별 ERF 성장 곡선, 224² 외 해상도, E3의 effective
attention 환원 — 전부 spec의 "범위 밖"과 일치한다.
